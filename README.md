# fastapi_model_wrapper
A simple wireframe for wrapping a ML model in a FastAPI app

## Install

    $ python -m venv venv
    $ source venv/bin/activate
    $ pip install -r requirements.txt
    
## Run
  
    $ uvicorn main:app --host 0.0.0.0 --port 80
  
  
## Test

    $ pytest
